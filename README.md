# STEGOC
Stegoc is a stegonography tool that quite simply appends the bytes of one file (hidden file) onto another (carrier file) and writes an output file. You can also extract a file from another file with this tool, however only files
that are hidden with the stegoc tool can be extracted. 